# pip install -r requirements.txt
# Ensure that pip installs at least the minor version you have specified (the second number),
# but it would not install a different major version (the first number) which might include breaking changes:
Flask~=2.1
google-cloud-logging~=3.5
requests~=2.31
gunicorn~=20.1
python-dotenv==1.0.0
pandas==2.1.3
tabulate==0.9.0
openai==0.28.1
langchain==0.0.309
langsmith==0.0.42


